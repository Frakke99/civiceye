# Zelfde project als de app. De anon key is publiek; de console werkt met het
# JWT van een moderatoraccount (profiles.trust_level >= 3), nooit met de
# service_role-key.
NEXT_PUBLIC_SUPABASE_URL=https://<ref>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon key>
